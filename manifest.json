{
  "name": "RingCentral extra Tab closer",
  "version": "1.0",
  "description": "Closes all the tabs that zoom and aws opens when fires a meeting from the calendar application.",
  "permissions": ["storage", "tabs"],
  "background": {
    "scripts": ["background.js"],
    "persistent": false
  },
  "manifest_version": 2
}
